apiVersion: apps/v1
kind: Deployment
metadata:
  name: slack-agent
  namespace: actions-system
spec:
  template:
    spec:
      containers:
      - name: agent
        image: actions-controller:kindtest
        imagePullPolicy: Never
