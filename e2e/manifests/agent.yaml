apiVersion: apps/v1
kind: Deployment
metadata:
  name: slack-agent
  namespace: actions-system
spec:
  template:
    spec:
      containers:
      - name: notifier
        imagePullPolicy: Never
        args:
          - --echo-back
      - name: extender
        imagePullPolicy: Never
